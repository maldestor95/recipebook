/**
 * @params recipePath path to local folder
 */
export const recipePath='recipe/'
/**
 *  @params recipePathUrl path to recipe folder on the repository
*/
export const recipePathUrl=`https://raw.githubusercontent.com/maldestor95/recipebook/master/recipe/`

export default {
   recipePath,
   recipePathUrl
}